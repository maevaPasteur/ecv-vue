const state = {
    albums: []
};

const mutations = {

};

const actions = {

};

const getters = {
    albums: state => state.albums
};

const albums = {
    state: state,
    mutations: mutations,
    actions: actions,
    getters: getters
};

export default albums